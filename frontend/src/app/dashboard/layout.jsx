"use client";

export default function RegistrationLayout({ children }) {
	return (
		<div
			style={{
				position: "relative",
				width: "100%",
				minHeight: "100vh",
			}}
		>
			{children}
		</div>
	);
}
