"use client";

export default function RegistrationLayout({ children }) {
	return (
		<div
			style={{
				position: "relative",
				width: "100%",
				height: "100vh",
				overflow: "hidden",
			}}
		>
			{children}
		</div>
	);
}
